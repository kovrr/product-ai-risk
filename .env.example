# AIKovrr Environment Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# SETUP MODE
# =============================================================================
# Choose your setup mode by uncommenting the appropriate section below:
# - Local: Run services directly on your machine
# - Docker: Run services in containers (recommended for consistency)

# =============================================================================
# Django Settings
# =============================================================================

# Secret key for cryptographic signing (REQUIRED in production)
# Generate a new one: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-rk$n-h@t(s#ib)14bkex1@o+^an-bddl9)nwjv21lf9f$2qury

# Debug mode (True/False) - MUST be False in production
DEBUG=True

# Comma-separated list of allowed hosts (* allows all - only for development)
ALLOWED_HOSTS=*

# =============================================================================
# Database Configuration
# =============================================================================

# PostgreSQL database name
DB_NAME=aikovrr

# PostgreSQL user
DB_USER=postgres

# PostgreSQL password
# Local development: leave empty if no password required
# Docker development: postgres (or any password)
# Production: use a strong password
DB_PASSWORD=

# PostgreSQL host
# Local development: localhost
# Docker development: db
# Production: your database host
DB_HOST=localhost

# PostgreSQL port (default: 5432)
DB_PORT=5432

# =============================================================================
# Docker-Specific Configuration
# =============================================================================

# Compose project name (used for container/volume naming)
COMPOSE_PROJECT_NAME=aikovrr

# Docker database password (used only when running with docker-compose)
# DOCKER_DB_PASSWORD=postgres

# =============================================================================
# Frontend Configuration
# =============================================================================

# API base URL for frontend
# Local development: http://localhost:8000/api
# Docker development: http://localhost:8000/api (backend exposed on host)
# Production: https://your-domain.com/api
VITE_API_URL=http://localhost:8000/api

# =============================================================================
# CORS Settings (Development Only)
# =============================================================================

# Allow all origins (True/False) - only for development
CORS_ALLOW_ALL_ORIGINS=True

# =============================================================================
# Quick Configuration Examples
# =============================================================================

# --- LOCAL DEVELOPMENT (without Docker) ---
# Use these values when running services directly on your machine:
#
# DB_HOST=localhost
# DB_USER=postgres
# DB_PASSWORD=
# VITE_API_URL=http://localhost:8000/api
#
# Then run: ./setup.sh

# --- DOCKER DEVELOPMENT ---
# Use these values when running with docker-compose:
#
# DB_HOST=db
# DB_USER=postgres
# DB_PASSWORD=postgres
# VITE_API_URL=http://localhost:8000/api
#
# Then run: docker-compose up

# --- DOCKER PRODUCTION ---
# Use these values for production deployment:
#
# SECRET_KEY=<generate-new-secret-key>
# DEBUG=False
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# DB_HOST=db
# DB_USER=postgres
# DB_PASSWORD=<strong-password>
# VITE_API_URL=https://yourdomain.com/api
#
# Then run: docker-compose -f docker-compose.prod.yml up -d

# =============================================================================
# Notes
# =============================================================================

# 1. Never commit the actual .env file to version control
# 2. .env is already in .gitignore
# 3. For production, generate a new SECRET_KEY
# 4. For production, set DEBUG=False
# 5. For production, specify exact ALLOWED_HOSTS (no wildcards)
# 6. Docker uses service names (e.g., 'db', 'backend') for inter-container communication
